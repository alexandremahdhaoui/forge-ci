<!-- Code generated by docgen. DO NOT EDIT. Source: cmd/ci-artifact-release/spec.yaml -->

# ci-artifact-release

**Tag every repo of a revision and publish a GitHub release.**

A forge-ci `artifact` engine.

## Why it exists

The artifact port was specified and empty, so a pipeline could prove a revision
and then publish nothing. This tags each member at the commit the revision names
and attaches the binaries forge already built.

## Tools

| Tool | Does |
|---|---|
| `declare` | Report the resources this release needs. It needs none. |
| `publish` | Tag every repo of the revision and create the release. |

## By hand

```sh
echo '{"revision":"3dd48e96ed7e","version":"v0.2.0","repos":{"golden-rust":"a5b26c6a"},"spec":{"root":".."}}' \
  | ci-artifact-release publish
```

## In a pipeline

```yaml
engines:
  - alias: my-artifact
    type: artifact
    engine: "go://github.com/alexandremahdhaoui/forge-ci/cmd/ci-artifact-release@v0.1.0"
    manager: local
    spec:
      releaseIn: <string>
      root: <string>
```

## Worth knowing

A dirty revision is refused. It was never proven, so releasing it publishes
something nobody can reproduce.

A tag that already exists is refused rather than moved, because a moved tag changes
what a consumer already pinned.

Only an artifact whose location is a file:// URL is uploaded. A container image is
published by whatever built it.
