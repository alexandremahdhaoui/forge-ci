// Code generated by forge-dev. DO NOT EDIT.
// Source: spec.openapi.yaml
// SourceChecksum: sha256:a6481ef2737c118d0bc6789839134a3af31aa862e2987d0077815e587eeca2c1

package main

import (
	"fmt"

	"github.com/alexandremahdhaoui/forge/pkg/mcptypes"
)

// ValidateForgeArtifact validates a ForgeArtifact and returns validation results.
// It checks required fields and validates enum values.
func ValidateForgeArtifact(s *ForgeArtifact) *mcptypes.ConfigValidateOutput {
	if s == nil {
		return &mcptypes.ConfigValidateOutput{
			Valid: true,
		}
	}

	var errors []mcptypes.ValidationError

	if len(errors) > 0 {
		return &mcptypes.ConfigValidateOutput{
			Valid:  false,
			Errors: errors,
		}
	}

	return &mcptypes.ConfigValidateOutput{
		Valid: true,
	}
}

// ValidateSpec validates a Spec and returns validation results.
// It checks required fields and validates enum values.
func ValidateSpec(s *Spec) *mcptypes.ConfigValidateOutput {
	if s == nil {
		return &mcptypes.ConfigValidateOutput{
			Valid: true,
		}
	}

	var errors []mcptypes.ValidationError

	if len(errors) > 0 {
		return &mcptypes.ConfigValidateOutput{
			Valid:  false,
			Errors: errors,
		}
	}

	return &mcptypes.ConfigValidateOutput{
		Valid: true,
	}
}

// ValidateArtifactOutput validates a ArtifactOutput and returns validation results.
// It checks required fields and validates enum values.
func ValidateArtifactOutput(s *ArtifactOutput) *mcptypes.ConfigValidateOutput {
	if s == nil {
		return &mcptypes.ConfigValidateOutput{
			Valid: true,
		}
	}

	var errors []mcptypes.ValidationError

	if len(errors) > 0 {
		return &mcptypes.ConfigValidateOutput{
			Valid:  false,
			Errors: errors,
		}
	}

	return &mcptypes.ConfigValidateOutput{
		Valid: true,
	}
}

// ValidateForgeResult validates a ForgeResult and returns validation results.
// It checks required fields and validates enum values.
func ValidateForgeResult(s *ForgeResult) *mcptypes.ConfigValidateOutput {
	if s == nil {
		return &mcptypes.ConfigValidateOutput{
			Valid: true,
		}
	}

	var errors []mcptypes.ValidationError

	if len(errors) > 0 {
		return &mcptypes.ConfigValidateOutput{
			Valid:  false,
			Errors: errors,
		}
	}

	return &mcptypes.ConfigValidateOutput{
		Valid: true,
	}
}

// ValidateOwnership validates a Ownership and returns validation results.
// It checks required fields and validates enum values.
func ValidateOwnership(s *Ownership) *mcptypes.ConfigValidateOutput {
	if s == nil {
		return &mcptypes.ConfigValidateOutput{
			Valid: true,
		}
	}

	var errors []mcptypes.ValidationError
	// Validate required field: manager
	if s.Manager == "" {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.manager",
			Message: "required field is missing",
		})
	}
	// Validate required field: resource
	if s.Resource == "" {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.resource",
			Message: "required field is missing",
		})
	}

	if len(errors) > 0 {
		return &mcptypes.ConfigValidateOutput{
			Valid:  false,
			Errors: errors,
		}
	}

	return &mcptypes.ConfigValidateOutput{
		Valid: true,
	}
}

// ValidatePromotionOutput validates a PromotionOutput and returns validation results.
// It checks required fields and validates enum values.
func ValidatePromotionOutput(s *PromotionOutput) *mcptypes.ConfigValidateOutput {
	if s == nil {
		return &mcptypes.ConfigValidateOutput{
			Valid: true,
		}
	}

	var errors []mcptypes.ValidationError
	// Validate required field: reason
	if s.Reason == "" {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.reason",
			Message: "required field is missing",
		})
	}

	if len(errors) > 0 {
		return &mcptypes.ConfigValidateOutput{
			Valid:  false,
			Errors: errors,
		}
	}

	return &mcptypes.ConfigValidateOutput{
		Valid: true,
	}
}

// ValidateRepoCheckout validates a RepoCheckout and returns validation results.
// It checks required fields and validates enum values.
func ValidateRepoCheckout(s *RepoCheckout) *mcptypes.ConfigValidateOutput {
	if s == nil {
		return &mcptypes.ConfigValidateOutput{
			Valid: true,
		}
	}

	var errors []mcptypes.ValidationError
	// Validate required field: name
	if s.Name == "" {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.name",
			Message: "required field is missing",
		})
	}
	// Validate required field: path
	if s.Path == "" {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.path",
			Message: "required field is missing",
		})
	}
	// Validate required field: sha
	if s.Sha == "" {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.sha",
			Message: "required field is missing",
		})
	}

	if len(errors) > 0 {
		return &mcptypes.ConfigValidateOutput{
			Valid:  false,
			Errors: errors,
		}
	}

	return &mcptypes.ConfigValidateOutput{
		Valid: true,
	}
}

// ValidateTarget validates a Target and returns validation results.
// It checks required fields and validates enum values.
func ValidateTarget(s *Target) *mcptypes.ConfigValidateOutput {
	if s == nil {
		return &mcptypes.ConfigValidateOutput{
			Valid: true,
		}
	}

	var errors []mcptypes.ValidationError
	// Validate required field: alias
	if s.Alias == "" {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.alias",
			Message: "required field is missing",
		})
	}

	if len(errors) > 0 {
		return &mcptypes.ConfigValidateOutput{
			Valid:  false,
			Errors: errors,
		}
	}

	return &mcptypes.ConfigValidateOutput{
		Valid: true,
	}
}

// ValidateTriggerOutput validates a TriggerOutput and returns validation results.
// It checks required fields and validates enum values.
func ValidateTriggerOutput(s *TriggerOutput) *mcptypes.ConfigValidateOutput {
	if s == nil {
		return &mcptypes.ConfigValidateOutput{
			Valid: true,
		}
	}

	var errors []mcptypes.ValidationError
	// Validate required field: fingerprint
	if s.Fingerprint == "" {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.fingerprint",
			Message: "required field is missing",
		})
	}

	if len(errors) > 0 {
		return &mcptypes.ConfigValidateOutput{
			Valid:  false,
			Errors: errors,
		}
	}

	return &mcptypes.ConfigValidateOutput{
		Valid: true,
	}
}

// ValidateArtifactInput validates a ArtifactInput and returns validation results.
// It checks required fields and validates enum values.
func ValidateArtifactInput(s *ArtifactInput) *mcptypes.ConfigValidateOutput {
	if s == nil {
		return &mcptypes.ConfigValidateOutput{
			Valid: true,
		}
	}

	var errors []mcptypes.ValidationError
	// Validate array of references: artifacts
	for i, item := range s.Artifacts {
		nestedResult := ValidateForgeArtifact(&item)
		if !nestedResult.Valid {
			for _, e := range nestedResult.Errors {
				errors = append(errors, mcptypes.ValidationError{
					Field:   fmt.Sprintf("spec.artifacts[%d].%s", i, e.Field),
					Message: e.Message,
				})
			}
		}
	}
	// Validate required field: revision
	if s.Revision == "" {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.revision",
			Message: "required field is missing",
		})
	}
	// Validate nested reference: spec
	{
		nested := s.Spec
		nestedResult := ValidateSpec(&nested)
		if !nestedResult.Valid {
			for _, e := range nestedResult.Errors {
				errors = append(errors, mcptypes.ValidationError{
					Field:   "spec.spec." + e.Field,
					Message: e.Message,
				})
			}
		}
	}
	// Validate required field: version
	if s.Version == "" {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.version",
			Message: "required field is missing",
		})
	}

	if len(errors) > 0 {
		return &mcptypes.ConfigValidateOutput{
			Valid:  false,
			Errors: errors,
		}
	}

	return &mcptypes.ConfigValidateOutput{
		Valid: true,
	}
}

// ValidateDeclareInput validates a DeclareInput and returns validation results.
// It checks required fields and validates enum values.
func ValidateDeclareInput(s *DeclareInput) *mcptypes.ConfigValidateOutput {
	if s == nil {
		return &mcptypes.ConfigValidateOutput{
			Valid: true,
		}
	}

	var errors []mcptypes.ValidationError
	// Validate nested reference: spec
	{
		nested := s.Spec
		nestedResult := ValidateSpec(&nested)
		if !nestedResult.Valid {
			for _, e := range nestedResult.Errors {
				errors = append(errors, mcptypes.ValidationError{
					Field:   "spec.spec." + e.Field,
					Message: e.Message,
				})
			}
		}
	}

	if len(errors) > 0 {
		return &mcptypes.ConfigValidateOutput{
			Valid:  false,
			Errors: errors,
		}
	}

	return &mcptypes.ConfigValidateOutput{
		Valid: true,
	}
}

// ValidateResource validates a Resource and returns validation results.
// It checks required fields and validates enum values.
func ValidateResource(s *Resource) *mcptypes.ConfigValidateOutput {
	if s == nil {
		return &mcptypes.ConfigValidateOutput{
			Valid: true,
		}
	}

	var errors []mcptypes.ValidationError
	// Validate required field: kind
	if s.Kind == "" {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.kind",
			Message: "required field is missing",
		})
	}
	// Validate required field: name
	if s.Name == "" {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.name",
			Message: "required field is missing",
		})
	}
	// Validate nested reference: spec
	{
		nested := s.Spec
		nestedResult := ValidateSpec(&nested)
		if !nestedResult.Valid {
			for _, e := range nestedResult.Errors {
				errors = append(errors, mcptypes.ValidationError{
					Field:   "spec.spec." + e.Field,
					Message: e.Message,
				})
			}
		}
	}

	if len(errors) > 0 {
		return &mcptypes.ConfigValidateOutput{
			Valid:  false,
			Errors: errors,
		}
	}

	return &mcptypes.ConfigValidateOutput{
		Valid: true,
	}
}

// ValidateGateResult validates a GateResult and returns validation results.
// It checks required fields and validates enum values.
func ValidateGateResult(s *GateResult) *mcptypes.ConfigValidateOutput {
	if s == nil {
		return &mcptypes.ConfigValidateOutput{
			Valid: true,
		}
	}

	var errors []mcptypes.ValidationError
	// Validate required field: alias
	if s.Alias == "" {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.alias",
			Message: "required field is missing",
		})
	}
	// Validate required enum field: status. An enum is a string.
	if s.Status == "" {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.status",
			Message: "required field is missing or empty",
		})
	}
	// Status is an enum. Its allowed values are checked where it is
	// declared, and there is no Validate function generated for a string type.

	if len(errors) > 0 {
		return &mcptypes.ConfigValidateOutput{
			Valid:  false,
			Errors: errors,
		}
	}

	return &mcptypes.ConfigValidateOutput{
		Valid: true,
	}
}

// ValidateRunOutput validates a RunOutput and returns validation results.
// It checks required fields and validates enum values.
func ValidateRunOutput(s *RunOutput) *mcptypes.ConfigValidateOutput {
	if s == nil {
		return &mcptypes.ConfigValidateOutput{
			Valid: true,
		}
	}

	var errors []mcptypes.ValidationError
	// Validate nested reference: forge
	{
		nested := s.Forge
		nestedResult := ValidateForgeResult(&nested)
		if !nestedResult.Valid {
			for _, e := range nestedResult.Errors {
				errors = append(errors, mcptypes.ValidationError{
					Field:   "spec.forge." + e.Field,
					Message: e.Message,
				})
			}
		}
	}
	// Validate required enum field: status. An enum is a string.
	if s.Status == "" {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.status",
			Message: "required field is missing or empty",
		})
	}
	// Status is an enum. Its allowed values are checked where it is
	// declared, and there is no Validate function generated for a string type.

	if len(errors) > 0 {
		return &mcptypes.ConfigValidateOutput{
			Valid:  false,
			Errors: errors,
		}
	}

	return &mcptypes.ConfigValidateOutput{
		Valid: true,
	}
}

// ValidateReconcileOutput validates a ReconcileOutput and returns validation results.
// It checks required fields and validates enum values.
func ValidateReconcileOutput(s *ReconcileOutput) *mcptypes.ConfigValidateOutput {
	if s == nil {
		return &mcptypes.ConfigValidateOutput{
			Valid: true,
		}
	}

	var errors []mcptypes.ValidationError
	// Validate required field: actions
	if len(s.Actions) == 0 {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.actions",
			Message: "required field is missing or empty",
		})
	}
	// Validate required array of references: owned
	if len(s.Owned) == 0 {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.owned",
			Message: "required field is missing or empty",
		})
	}
	// Validate array of references: owned
	for i, item := range s.Owned {
		nestedResult := ValidateOwnership(&item)
		if !nestedResult.Valid {
			for _, e := range nestedResult.Errors {
				errors = append(errors, mcptypes.ValidationError{
					Field:   fmt.Sprintf("spec.owned[%d].%s", i, e.Field),
					Message: e.Message,
				})
			}
		}
	}

	if len(errors) > 0 {
		return &mcptypes.ConfigValidateOutput{
			Valid:  false,
			Errors: errors,
		}
	}

	return &mcptypes.ConfigValidateOutput{
		Valid: true,
	}
}

// ValidateRunInput validates a RunInput and returns validation results.
// It checks required fields and validates enum values.
func ValidateRunInput(s *RunInput) *mcptypes.ConfigValidateOutput {
	if s == nil {
		return &mcptypes.ConfigValidateOutput{
			Valid: true,
		}
	}

	var errors []mcptypes.ValidationError
	// Validate array of references: repos
	for i, item := range s.Repos {
		nestedResult := ValidateRepoCheckout(&item)
		if !nestedResult.Valid {
			for _, e := range nestedResult.Errors {
				errors = append(errors, mcptypes.ValidationError{
					Field:   fmt.Sprintf("spec.repos[%d].%s", i, e.Field),
					Message: e.Message,
				})
			}
		}
	}
	// Validate required field: revision
	if s.Revision == "" {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.revision",
			Message: "required field is missing",
		})
	}
	// Validate required field: root
	if s.Root == "" {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.root",
			Message: "required field is missing",
		})
	}
	// Validate nested reference: spec
	{
		nested := s.Spec
		nestedResult := ValidateSpec(&nested)
		if !nestedResult.Valid {
			for _, e := range nestedResult.Errors {
				errors = append(errors, mcptypes.ValidationError{
					Field:   "spec.spec." + e.Field,
					Message: e.Message,
				})
			}
		}
	}
	// Validate required field: stage
	if s.Stage == "" {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.stage",
			Message: "required field is missing",
		})
	}
	// Validate required field: substage
	if s.Substage == "" {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.substage",
			Message: "required field is missing",
		})
	}
	// Validate required array of references: targets
	if len(s.Targets) == 0 {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.targets",
			Message: "required field is missing or empty",
		})
	}
	// Validate array of references: targets
	for i, item := range s.Targets {
		nestedResult := ValidateTarget(&item)
		if !nestedResult.Valid {
			for _, e := range nestedResult.Errors {
				errors = append(errors, mcptypes.ValidationError{
					Field:   fmt.Sprintf("spec.targets[%d].%s", i, e.Field),
					Message: e.Message,
				})
			}
		}
	}

	if len(errors) > 0 {
		return &mcptypes.ConfigValidateOutput{
			Valid:  false,
			Errors: errors,
		}
	}

	return &mcptypes.ConfigValidateOutput{
		Valid: true,
	}
}

// ValidateDeclareOutput validates a DeclareOutput and returns validation results.
// It checks required fields and validates enum values.
func ValidateDeclareOutput(s *DeclareOutput) *mcptypes.ConfigValidateOutput {
	if s == nil {
		return &mcptypes.ConfigValidateOutput{
			Valid: true,
		}
	}

	var errors []mcptypes.ValidationError
	// Validate required array of references: resources
	if len(s.Resources) == 0 {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.resources",
			Message: "required field is missing or empty",
		})
	}
	// Validate array of references: resources
	for i, item := range s.Resources {
		nestedResult := ValidateResource(&item)
		if !nestedResult.Valid {
			for _, e := range nestedResult.Errors {
				errors = append(errors, mcptypes.ValidationError{
					Field:   fmt.Sprintf("spec.resources[%d].%s", i, e.Field),
					Message: e.Message,
				})
			}
		}
	}

	if len(errors) > 0 {
		return &mcptypes.ConfigValidateOutput{
			Valid:  false,
			Errors: errors,
		}
	}

	return &mcptypes.ConfigValidateOutput{
		Valid: true,
	}
}

// ValidateReconcileInput validates a ReconcileInput and returns validation results.
// It checks required fields and validates enum values.
func ValidateReconcileInput(s *ReconcileInput) *mcptypes.ConfigValidateOutput {
	if s == nil {
		return &mcptypes.ConfigValidateOutput{
			Valid: true,
		}
	}

	var errors []mcptypes.ValidationError
	// Validate required field: manager
	if s.Manager == "" {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.manager",
			Message: "required field is missing",
		})
	}
	// Validate array of references: owned
	for i, item := range s.Owned {
		nestedResult := ValidateOwnership(&item)
		if !nestedResult.Valid {
			for _, e := range nestedResult.Errors {
				errors = append(errors, mcptypes.ValidationError{
					Field:   fmt.Sprintf("spec.owned[%d].%s", i, e.Field),
					Message: e.Message,
				})
			}
		}
	}
	// Validate required array of references: resources
	if len(s.Resources) == 0 {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.resources",
			Message: "required field is missing or empty",
		})
	}
	// Validate array of references: resources
	for i, item := range s.Resources {
		nestedResult := ValidateResource(&item)
		if !nestedResult.Valid {
			for _, e := range nestedResult.Errors {
				errors = append(errors, mcptypes.ValidationError{
					Field:   fmt.Sprintf("spec.resources[%d].%s", i, e.Field),
					Message: e.Message,
				})
			}
		}
	}
	// Validate nested reference: spec
	{
		nested := s.Spec
		nestedResult := ValidateSpec(&nested)
		if !nestedResult.Valid {
			for _, e := range nestedResult.Errors {
				errors = append(errors, mcptypes.ValidationError{
					Field:   "spec.spec." + e.Field,
					Message: e.Message,
				})
			}
		}
	}

	if len(errors) > 0 {
		return &mcptypes.ConfigValidateOutput{
			Valid:  false,
			Errors: errors,
		}
	}

	return &mcptypes.ConfigValidateOutput{
		Valid: true,
	}
}

// ValidateRun validates a Run and returns validation results.
// It checks required fields and validates enum values.
func ValidateRun(s *Run) *mcptypes.ConfigValidateOutput {
	if s == nil {
		return &mcptypes.ConfigValidateOutput{
			Valid: true,
		}
	}

	var errors []mcptypes.ValidationError
	// Validate required field: engine
	if s.Engine == "" {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.engine",
			Message: "required field is missing",
		})
	}
	// Validate nested reference: forge
	{
		nested := s.Forge
		nestedResult := ValidateForgeResult(&nested)
		if !nestedResult.Valid {
			for _, e := range nestedResult.Errors {
				errors = append(errors, mcptypes.ValidationError{
					Field:   "spec.forge." + e.Field,
					Message: e.Message,
				})
			}
		}
	}
	// Validate array of references: gates
	for i, item := range s.Gates {
		nestedResult := ValidateGateResult(&item)
		if !nestedResult.Valid {
			for _, e := range nestedResult.Errors {
				errors = append(errors, mcptypes.ValidationError{
					Field:   fmt.Sprintf("spec.gates[%d].%s", i, e.Field),
					Message: e.Message,
				})
			}
		}
	}
	// Validate required field: revision
	if s.Revision == "" {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.revision",
			Message: "required field is missing",
		})
	}
	// Validate required field: stage
	if s.Stage == "" {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.stage",
			Message: "required field is missing",
		})
	}
	// Validate required field: startedAt
	if s.StartedAt == "" {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.startedAt",
			Message: "required field is missing",
		})
	}
	// Validate required enum field: status. An enum is a string.
	if s.Status == "" {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.status",
			Message: "required field is missing or empty",
		})
	}
	// Status is an enum. Its allowed values are checked where it is
	// declared, and there is no Validate function generated for a string type.
	// Validate required field: substage
	if s.Substage == "" {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.substage",
			Message: "required field is missing",
		})
	}

	if len(errors) > 0 {
		return &mcptypes.ConfigValidateOutput{
			Valid:  false,
			Errors: errors,
		}
	}

	return &mcptypes.ConfigValidateOutput{
		Valid: true,
	}
}

// ValidateGateInput validates a GateInput and returns validation results.
// It checks required fields and validates enum values.
func ValidateGateInput(s *GateInput) *mcptypes.ConfigValidateOutput {
	if s == nil {
		return &mcptypes.ConfigValidateOutput{
			Valid: true,
		}
	}

	var errors []mcptypes.ValidationError
	// Validate required reference field: run
	// Validate nested reference: run
	{
		nested := s.Run
		nestedResult := ValidateRun(&nested)
		if !nestedResult.Valid {
			for _, e := range nestedResult.Errors {
				errors = append(errors, mcptypes.ValidationError{
					Field:   "spec.run." + e.Field,
					Message: e.Message,
				})
			}
		}
	}
	// Validate nested reference: spec
	{
		nested := s.Spec
		nestedResult := ValidateSpec(&nested)
		if !nestedResult.Valid {
			for _, e := range nestedResult.Errors {
				errors = append(errors, mcptypes.ValidationError{
					Field:   "spec.spec." + e.Field,
					Message: e.Message,
				})
			}
		}
	}

	if len(errors) > 0 {
		return &mcptypes.ConfigValidateOutput{
			Valid:  false,
			Errors: errors,
		}
	}

	return &mcptypes.ConfigValidateOutput{
		Valid: true,
	}
}

// ValidatePromotionInput validates a PromotionInput and returns validation results.
// It checks required fields and validates enum values.
func ValidatePromotionInput(s *PromotionInput) *mcptypes.ConfigValidateOutput {
	if s == nil {
		return &mcptypes.ConfigValidateOutput{
			Valid: true,
		}
	}

	var errors []mcptypes.ValidationError
	// Validate required array of references: runs
	if len(s.Runs) == 0 {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.runs",
			Message: "required field is missing or empty",
		})
	}
	// Validate array of references: runs
	for i, item := range s.Runs {
		nestedResult := ValidateRun(&item)
		if !nestedResult.Valid {
			for _, e := range nestedResult.Errors {
				errors = append(errors, mcptypes.ValidationError{
					Field:   fmt.Sprintf("spec.runs[%d].%s", i, e.Field),
					Message: e.Message,
				})
			}
		}
	}
	// Validate nested reference: spec
	{
		nested := s.Spec
		nestedResult := ValidateSpec(&nested)
		if !nestedResult.Valid {
			for _, e := range nestedResult.Errors {
				errors = append(errors, mcptypes.ValidationError{
					Field:   "spec.spec." + e.Field,
					Message: e.Message,
				})
			}
		}
	}
	// Validate required field: stage
	if s.Stage == "" {
		errors = append(errors, mcptypes.ValidationError{
			Field:   "spec.stage",
			Message: "required field is missing",
		})
	}

	if len(errors) > 0 {
		return &mcptypes.ConfigValidateOutput{
			Valid:  false,
			Errors: errors,
		}
	}

	return &mcptypes.ConfigValidateOutput{
		Valid: true,
	}
}

// Validate validates a Spec and returns validation results.
// This is the main entry point for validation.
func Validate(s *Spec) *mcptypes.ConfigValidateOutput {
	return ValidateSpec(s)
}

// ValidateMap validates a map[string]interface{} by first parsing it to a Spec.
// This is a convenience function for validating spec data from forge.yaml.
func ValidateMap(m map[string]interface{}) *mcptypes.ConfigValidateOutput {
	s, err := FromMap(m)
	if err != nil {
		return &mcptypes.ConfigValidateOutput{
			Valid: false,
			Errors: []mcptypes.ValidationError{
				{
					Field:   "spec",
					Message: err.Error(),
				},
			},
		}
	}

	return Validate(s)
}
