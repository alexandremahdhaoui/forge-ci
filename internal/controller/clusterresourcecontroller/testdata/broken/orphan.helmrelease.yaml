apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: orphan
  namespace: demo
spec:
  chart:
    spec:
      chart: orphan
      version: 1.0.0
      sourceRef:
        kind: HelmRepository
        name: nowhere
        namespace: demo
