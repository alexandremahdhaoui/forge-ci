apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: urlless
  namespace: demo
spec:
  chart:
    spec:
      chart: urlless
      version: 1.0.0
      sourceRef:
        kind: HelmRepository
        name: urlless
        namespace: demo
