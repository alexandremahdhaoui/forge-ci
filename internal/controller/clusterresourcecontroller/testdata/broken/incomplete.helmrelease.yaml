apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: incomplete
  namespace: demo
spec:
  chart:
    spec:
      chart: incomplete
      sourceRef:
        kind: HelmRepository
        name: demo
        namespace: demo
