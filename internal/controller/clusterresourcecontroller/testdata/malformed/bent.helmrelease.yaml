apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: bent
  namespace: demo
spec:
  chart:
    spec:
      chart: bent
      version: 1.0.0
      sourceRef:
        kind: HelmRepository
        name: bent
        namespace: demo
